package env

// API
const (
	API_PORT string = "API_PORT"
)

// Cache - Redis
const (
	REDIS_PORT string = "REDIS_PORT"
)

const (
	SECRET_KEY string = "SECRET_KEY"
)

// Swagger host
const (
	SWAGGER_HOST string = "SWAGGER_HOST"
)
